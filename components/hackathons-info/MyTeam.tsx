import * as React from 'react';

interface MyTeamProps {
  propName: type;
}

const ComponentName: React.FC<MyTeamProps> = ({ propName }) => {
  return (
    <div>
      
    </div>
  );
};

export default ComponentName;